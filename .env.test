# 测试环境配置文件
# 使用内嵌SurrealDB数据库，无需外部连接

# 数据库访问模式 - 测试环境使用直连模式
VITE_DB_ACCESS_MODE=direct

# 内嵌数据库配置（仅用于测试）
VITE_SURREALDB_WS_URL=memory://
VITE_SURREALDB_NS=test_namespace  
VITE_SURREALDB_DB=test_database

# API 和其他服务（测试环境可以使用 mock）
VITE_API_URL=http://localhost:3000/api
VITE_TURNSTILE_SITE_KEY=test_site_key

# 测试标识
NODE_ENV=test
VITE_NODE_ENV=test